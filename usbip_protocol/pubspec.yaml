name: usbip_protocol
description: USB/IP protocol v1.1.1 message serialization — shared by Mac client and Windows server.
version: 1.0.0

environment:
  sdk: ^3.11.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
