name: mac_usb_forwarder
description: Mac USB Forwarder — connects to an existing NoPorts SSH tunnel and streams USB data.
version: 1.0.0

environment:
  sdk: ^3.11.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
