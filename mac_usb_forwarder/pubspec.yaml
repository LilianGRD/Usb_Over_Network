name: mac_usb_forwarder
description: Mac USB Forwarder — captures USB devices and forwards data over TCP tunnel.
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  ffi: ^2.1.0
  usbip_protocol:
    path: ../usbip_protocol

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
